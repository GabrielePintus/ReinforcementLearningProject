name: pusher
channels:
  - conda-forge
  - pytorch
dependencies:
  - python=3.11
  - jupyter
  - numpy  
  - tqdm
  - matplotlib
  - pip
  - pip:
      - scikit-learn
      - umap-learn
      - pyldavis
      - torch
      - gymnasium
      - gymnasium[mujoco] # For pusher env
      - gymnasium[other]